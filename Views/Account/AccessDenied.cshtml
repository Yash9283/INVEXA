@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Access Denied";
}

<div class="text-center py-5">
    <i class="bi bi-shield-lock text-danger" style="font-size:3.5rem;"></i>
    <h3 class="mt-3 fw-bold">Access Denied</h3>
    <p class="text-muted">You don't have permission to access this page.<br/>This action requires Administrator privileges.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-2">
        <i class="bi bi-house me-1"></i>Back to Dashboard
    </a>
</div>
